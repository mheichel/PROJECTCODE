        var currentLayer;
        var intervals = {{ json.dumps(intervals.tolist()) }};
        var pll_intervals = {{ json.dumps(pll_intervals.tolist()) }};
        var vintages = {{ json.dumps(vintages.tolist()) }};
        var columns = {{ json.dumps(columns[3:13]) }};
