# Add options to the dropdown menu
for col in columns[3:]:
    avg_col_name = f'avg_{col.split()[3].replace("(", "").replace(")", "").lower()}'
    month_label = col.split()[3]
    html_template += f'<option value="{avg_col_name}">{month_label}</option>\n'
