        # Add space between plots
        if idx < len(columns_to_plot) - 1:
            axis.spines['right'].set_visible(False)
            axes[idx + 1].spines['left'].set_visible(False)
            axis.set_xlim(axis.get_xlim()[0] - 0.5, axis.get_xlim()[1])
